# Value Discovery Guide — Full Package with Entity Evidence

This is a full deployment package. It includes:
- structured Supporting Entity Notes with:
  - Source
  - Confidence
  - Evidence Type
- full aligned front-end and back-end files

Render:
Build Command: npm install
Start Command: npm start

Environment variables:
OPENAI_API_KEY=your_project_api_key
OPENAI_MODEL=gpt-4.1-mini
