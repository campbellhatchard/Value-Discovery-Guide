# Value Discovery Guide — CRM Handoff Full Deployment Package

Deploy as a Render Web Service.

Build Command:
npm install

Start Command:
npm start

Environment variables:
OPENAI_API_KEY=your_project_api_key
OPENAI_MODEL=gpt-4.1-mini
